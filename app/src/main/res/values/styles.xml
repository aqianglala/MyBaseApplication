<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="theme_NoTitleBar" parent="@android:style/Theme.NoTitleBar">
        <item name="android:windowBackground">@color/color_background_gray</item>
        <item name="android:configChanges">keyboardHidden|orientation</item>
        <item name="android:textColor">@color/color_font_default</item>
        <item name="android:screenOrientation">landscape</item>
    </style>

    <style name="theme_TitleBackground">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="theme_TitleBar" parent="@android:style/Theme">
        <item name="android:windowTitleSize">?attr/actionBarSize</item>
        <item name="android:windowTitleBackgroundStyle">@style/theme_TitleBackground</item>
        <item name="android:windowBackground">@color/color_background_gray</item>
        <item name="android:configChanges">keyboardHidden|orientation</item>
        <item name="android:windowSoftInputMode">adjustUnspecified|stateAlwaysHidden</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:textColor">@color/color_font_default</item>
        <item name="android:listViewStyle">@android:style/Widget.ListView</item>
    </style>

</resources>
